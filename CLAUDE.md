# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a Java WebSocket library project (EasyWebsocket). The project is in early stages with no source code committed yet.

## Technology

- **Language**: Java
- **Target JDK**: 1.8
- **Build output**: `out/` directory
- **License**: Apache License 2.0

## Build Commands

No build system configured yet. When adding build configuration:
- Use Maven (`pom.xml`) or Gradle (`build.gradle`) for dependency management
- Java source files should be placed in `src/`